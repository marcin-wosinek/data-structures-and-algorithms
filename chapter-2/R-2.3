Button:
Class that encapsulate the button.

Methods:
on('<event-name', callback) - adds callbacks to the element events
disable() - change button state to inactive
enable() - change button state to active
setLabel('<new label>') - change the button label
